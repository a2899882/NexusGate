{
  "name": "nexusgate",
  "version": "0.6.7",
  "private": true,
  "description": "Lightweight multi-relay, multi-exit proxy orchestration control plane",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check agent/agent.js && node --check public/app.js && node --check scripts/reset-password.js && bash -n scripts/nexusgate.sh"
  },
  "license": "MIT"
}
